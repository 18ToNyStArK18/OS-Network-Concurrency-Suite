The given code is doing the following:
    a)As soon as the exec runs the code is calling the uvmalloc and load function located in kerner/exec.c and kexec function
    b)For changing the heap we use the sbrk mainly when we call the malloc function
    c)For the stack initially in the kexec the some size is allocated and that is fixed if we need more stack data then it will cause some trap and the kernel terminates the process

What we need to change:
    a)First while running the exec(kexec func) it should not allocate and load data (should not use uvmalloc and the load functions) we should just give assurance that I just gave permission for  this much data for you and when you need the unallocated data i will allocate the data
    b)For changing the heap size also we need to use the lazyloading like just say that you have this much spce right now and when you access data i will allocate that data
    c)For the stack its not fixed right now we should create a logic for this to expand aswell
    d)So in our new pagefault handler we need to do all these -> do smthg for the allocating the memory for the codes data and stack and heap
      If there is a pagefault then there are three reasons Stack , Heap and the data there is smthg called r_stval() which will give the faulting address so using this and comparing with the stack pointer the end_data and the heap_start we can find what are we supposed to doing
    e)So in our new handler we should first find why we got the pagefault after that we allocate the memory

Things I changed:
    a) in exec.c/kexec func instead of allocating the mem using the uvmalloc create a new function ulazymalloc and commet the load part
    b) in trap.c i created a new fucntion called page_handler whhich will ahndle the pagefault and remove dthe vmfault which was the default page)fault handler
    c) in vm.c i added that page_handler and removed that vm_handler
    d) i changed the copyin copyout and copyinstr which was using the old vmfault
    e) changed the kfork so that it works with the lazyloading 


after the part1
    the code does the following if kalloc will not allocate any memory it simply kills the process
    but we need to change that using the page replacement policy 
    we are asked to implement the fifo
    so when the calloc returns 0 we need to find the oldest allocated memory store in disk space and free the page so that the kalloc uses this 
    this should trigger only when the kalloc returns 0(memory cant be allocated)
    there should be a dirty bit which will become 1 when there is any write in that page if there is no such thing just dicard that data 
    *** no other process can evict the pages corresponding to the current process ***
    create a set or smtg which all set of theprocess virtual page that are curently loaded into the replacement
    it should fillow fifo

Things i am changing:
    added two macros like MAXPATH 128 and MAX_RESIDENT_PAGES 30k
    added max_swap_space as 1024 given in the question
    now the main changes i did are create the directory as soon as a process starts and rm the dir as soon as the process ends
    i created a array of size 1024 in the prc struct which stores the location of the pages which are swapped
    if when i call kalloc and it returned 0 then this implies that the physical memory is full and we  need to swap one page which we are not using 
    this eviction is using fifo and for loading there is a flag called is_swapped if this is true take the page from the swapspace else load it from the process data in the disk
    while evicting if there is any changes in the page then dirty flag is set and if dirty bit is set move it into the swap space else just evict it because next time we need this data we can get that from the data from disk  

gemini gave some weird logic to delete the file but that doesnt works
the file size is the issue
i need to change the kfork so that i copy all the parents data while creating a child
